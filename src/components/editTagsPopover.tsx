import { Container } from "@material-ui/core"
import React from "react"

const EditTagsPopover = ({ closePopover }: { closePopover: () => void }) => {
  return <Container maxWidth="xs">tags</Container>
}

export default EditTagsPopover
